The file "h_b_30hz_0.mat" contains source data and has 800 samples per class, whereas all other files contrains 400 samples per class and represents target data files

The source data file contains a struct variable 'Y' that contains train and test split data, whereas all target data files contains 'Y' that has split data: train-test-val